/* --- GLOBAL THEME & FONTS --- */
@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400&family=Playfair+Display:ital,wght@0,700;1,700&display=swap');

:root {
    --bg-main: #fcfaf7;
    --text-dark: #2c2c2c;
    --accent-gold: #c5a059;
    --font-fancy: 'Playfair Display', serif;
    --font-modern: 'Montserrat', sans-serif;
    --timeline-bg: #1a1a1a;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    font-family: var(--font-modern);
    background-color: var(--bg-main);
    color: var(--text-dark);
    display: flex;
    overflow-x: hidden;
}

/* --- THEMATIC CHAPTER OVERRIDES --- */
.theme-native { --chapter-accent: #8d6e63; --chapter-bg: #f5f1ed; }
.theme-roots { --chapter-accent: #5d4037; --chapter-bg: #fdf5e6; }
.theme-mansion { --chapter-accent: #704214; --chapter-bg: #f9f9f9; }
.theme-railroad { --chapter-accent: #34495e; --chapter-bg: #ecf0f1; }
.theme-music { --chapter-accent: #e91e63; --chapter-bg: #fff0f5; } /* Pop/Pink theme */
.theme-edu { --chapter-accent: #2980b9; --chapter-bg: #ffffff; }

/* --- LAYOUT --- */
#timeline-sidebar {
    width: 280px;
    height: 100vh;
    position: fixed;
    background: var(--timeline-bg);
    color: white;
    padding: 40px 20px;
    border-right: 5px solid var(--accent-gold);
}

main {
    margin-left: 280px;
    width: 100%;
}

section {
    min-height: 100vh;
    padding: 100px 10%;
    background-color: var(--chapter-bg);
    border-bottom: 1px solid rgba(0,0,0,0.05);
}

h1 {
    font-family: var(--font-fancy);
    font-size: 4rem;
    color: var(--chapter-accent);
    margin-bottom: 1rem;
    font-style: italic;
}

h2 {
    font-family: var(--font-fancy);
    font-size: 2rem;
    margin-top: 2rem;
    color: var(--text-dark);
}

p {
    line-height: 1.8;
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
    max-width: 800px;
}

/* --- INTERACTIVE MAP --- */
.map-frame {
    width: 100%;
    height: 500px;
    background: #ddd;
    border-radius: 15px;
    position: relative;
    border: 3px solid var(--accent-gold);
}
